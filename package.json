{
  "name": "toit-digital-ecosystem",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo for TOIT digital ecosystem with frontend and backend services.",
  "workspaces": [
    "apps/frontend",
    "apps/backend"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "dev": "npm run dev --workspaces",
    "format": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "3.3.2"
  }
}
